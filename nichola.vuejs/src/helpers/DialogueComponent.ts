import { DialogueGraph, DialogueType, DialogueNode } from '@/models/DialogueGraph'

export class DialogueComponent {
    static getNextDialogue(currentDialogueMessage: DialogueNode | null) {

        return null;
    }
}